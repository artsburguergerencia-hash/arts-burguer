version: '3.8'

services:
  artsburguer-backend:
    build: .
    container_name: artsburguer_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"